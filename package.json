{
  "name": "openclaw-desktop",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 1420",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "prepare:openclaw-bundle": "node scripts/prepare-openclaw-bundle.mjs",
    "generate:updater-manifest": "node scripts/generate-updater-manifest.mjs",
    "test:offline-local-codex-ui": "node scripts/test-offline-local-codex-ui.mjs",
    "test:windows-portable": "node scripts/test-windows-portable-install.mjs",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "npm run prepare:openclaw-bundle && tauri build",
    "build:mac": "npm run prepare:openclaw-bundle && tauri build --target universal-apple-darwin",
    "build:mac-intel": "npm run prepare:openclaw-bundle && tauri build --target x86_64-apple-darwin",
    "build:mac-arm": "npm run prepare:openclaw-bundle && tauri build --target aarch64-apple-darwin",
    "build:windows": "npm run prepare:openclaw-bundle && tauri build --target x86_64-msi",
    "build:all": "npm run prepare:openclaw-bundle && tauri build --target universal-apple-darwin,x86_64-msi"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.3.0",
    "@tauri-apps/plugin-process": "^2.3.1",
    "@tauri-apps/plugin-updater": "^2.10.0",
    "i18next": "^24.2.2",
    "lucide-react": "^1.23.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "react-i18next": "^15.4.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.3.1",
    "@types/react": "^19.0.10",
    "@types/react-dom": "^19.0.4",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "~5.7.2",
    "vite": "^6.1.0"
  }
}
